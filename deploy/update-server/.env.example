# Copy to .env (which is NOT committed) and fill in.

# Fine-grained PAT with READ-ONLY "Contents" permission on the private source repo only.
# Lives on the server; never shipped to clients.
GITHUB_TOKEN=github_pat_xxx

# Optional, if running cloudflared in the same compose stack.
# TUNNEL_TOKEN=eyJ...
