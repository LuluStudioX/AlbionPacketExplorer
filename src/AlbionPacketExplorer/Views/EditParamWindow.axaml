<controls:ApxWindow xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:AlbionPacketExplorer.Controls"
        xmlns:vm="using:AlbionPacketExplorer.ViewModels"
        xmlns:system="clr-namespace:System;assembly=System.Runtime"
        x:Class="AlbionPacketExplorer.Views.EditParamWindow"
        x:DataType="vm:EditParamViewModel"
        Title="{Binding Title}"
        Width="420" Height="400"
        MinWidth="320" MinHeight="380"
        CanResize="False"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">

    <StackPanel Margin="20" Spacing="14">

        <StackPanel Spacing="4">
            <StackPanel Orientation="Horizontal" Spacing="6">
                <TextBlock Text="Param name" FontSize="12" Foreground="Gray" VerticalAlignment="Center" />
                <Border Background="{DynamicResource ThemeAccentBrush}" CornerRadius="3" Padding="4,1"
                        IsVisible="{Binding SourceIsBase}" ToolTip.Tip="Name comes from base schema (SAT-derived)">
                    <TextBlock Text="base" FontSize="10" Foreground="{DynamicResource Apx.AccentText}" />
                </Border>
                <Border Background="{DynamicResource HighlightBrush}" CornerRadius="3" Padding="4,1"
                        IsVisible="{Binding SourceIsUser}" ToolTip.Tip="Name is your custom override">
                    <TextBlock Text="user" FontSize="10" Foreground="{DynamicResource HighlightForegroundBrush}" />
                </Border>
            </StackPanel>
            <TextBox Text="{Binding ParamName}"
                     PlaceholderText="e.g. objectId"
                     MaxLength="64" />
            <Border IsVisible="{Binding ShowSuggestions}"
                    Background="{DynamicResource Apx.Card}"
                    BorderBrush="{DynamicResource ThemeAccentBrush}"
                    BorderThickness="1" CornerRadius="4" MaxHeight="160"
                    ClipToBounds="True">
                <ListBox x:Name="SuggestionList"
                         ItemsSource="{Binding Suggestions}"
                         Background="Transparent"
                         BorderThickness="0"
                         SelectionMode="Single"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         SelectionChanged="OnSuggestionSelected">
                    <ListBox.Styles>
                        <Style Selector="ListBox">
                            <Setter Property="Background" Value="Transparent" />
                        </Style>
                        <Style Selector="ListBox > ScrollViewer">
                            <Setter Property="Background" Value="Transparent" />
                        </Style>
                        <Style Selector="ListBoxItem">
                            <Setter Property="Cursor" Value="Hand" />
                            <Setter Property="Padding" Value="6,4" />
                            <Setter Property="Foreground" Value="{DynamicResource Apx.Text}" />
                            <Setter Property="Background" Value="Transparent" />
                            <Setter Property="FontSize" Value="12" />
                        </Style>
                        <Style Selector="ListBoxItem:pointerover /template/ ContentPresenter">
                            <Setter Property="Background" Value="{DynamicResource ThemeAccentBrush}" />
                        </Style>
                        <Style Selector="ListBoxItem:selected /template/ ContentPresenter">
                            <Setter Property="Background" Value="{DynamicResource ThemeAccentBrush}" />
                        </Style>
                    </ListBox.Styles>
                </ListBox>
            </Border>
        </StackPanel>

        <StackPanel Spacing="4">
            <TextBlock Text="Note (shown on hover)" FontSize="12" Foreground="Gray" />
            <TextBox Text="{Binding Note}"
                     PlaceholderText="e.g. .NET ticks, 0=none 1=owner 2=guild"
                     MaxLength="256"
                     AcceptsReturn="False" />
        </StackPanel>

        <StackPanel Spacing="4">
            <TextBlock Text="Resolve as" FontSize="12" Foreground="Gray" />
            <ComboBox SelectedItem="{Binding ResolveAs}" HorizontalAlignment="Stretch"
                      ItemsSource="{x:Static vm:EditParamViewModel.ResolveAsOptions}" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right" Margin="0,6,0,0">
            <Button Content="Clear override"
                    Command="{Binding ClearOverrideCommand}"
                    Foreground="Gray" />
            <Button Content="Save"
                    Command="{Binding SaveCommand}"
                    Background="#2a5a2a" Foreground="White" />
        </StackPanel>

    </StackPanel>
</controls:ApxWindow>
