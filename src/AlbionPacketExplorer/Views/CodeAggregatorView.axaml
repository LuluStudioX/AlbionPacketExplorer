<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:sukiTheme="https://github.com/kikipoulet/SukiUI"
             xmlns:vm="using:AlbionPacketExplorer.ViewModels"
             x:Class="AlbionPacketExplorer.Views.CodeAggregatorView"
             x:DataType="vm:CodeAggregatorViewModel">

    <DockPanel>

        <DataGrid ItemsSource="{Binding CodeStats}"
                  FontSize="12"
                  RowHeight="24"
                  SelectedItem="{Binding SelectedRow, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserSortColumns="True"
                  CanUserResizeColumns="True"
                  GridLinesVisibility="Horizontal">
            <DataGrid.Styles>
                <Style Selector="DataGridCell">
                    <Setter Property="FontSize" Value="12" />
                </Style>
            </DataGrid.Styles>
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Copy Keys Summary"          Command="{Binding CopyKeySummaryCommand}" />
                    <MenuItem Header="Export C# Constructor Stub" Command="{Binding ExportCommand}" />
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Kind"  Binding="{Binding Kind}"       Width="80" />
                <DataGridTextColumn Header="Code"  Binding="{Binding Code}"       Width="60" />
                <DataGridTextColumn Header="Count" Binding="{Binding Count}"      Width="80" />
                <DataGridTextColumn Header="Keys"  Binding="{Binding KeySummary}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>

</UserControl>
